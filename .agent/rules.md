---
description: Git 提交信息生成规则（日报口吻、Conventional Commits、简体中文）
alwaysApply: true
---

# Rules for AI: Git Commit Message Generation

**主要目的**：生成可直接用于日报的提交信息，口吻与日报一致，便于复制到日报。

1.  **语言**：一律使用 **简体中文**。
2.  **格式**：遵循 Conventional Commits，格式为 `<type>: <subject>`。
3.  **分析**：根据代码 diff 判断合适的 `<type>`。
4.  **口吻**：采用 **日报口吻**——以「完成了…」「并完成…」等已完成工作表述，可带联调/对接/自测等收尾。

## 类型定义 (Type)
- **feat**: 新增功能
- **fix**: 修复 Bug
- **docs**: 文档变更
- **style**: 代码格式/样式调整
- **refactor**: 代码重构
- **perf**: 性能优化
- **test**: 测试相关
- **build**: 构建/依赖变更
- **ci**: CI 配置变更
- **chore**: 其他杂项
- **revert**: 回退代码

## 指导原则
- **日报口吻**：subject 用「完成了…」「并完成…」等表述，可带「并完成联调/对接/自测」等收尾。
- **字数与改动量挂钩**：
  - 改动少（如单文件、少量接口/类型）：约 **50～80 字**，一句话概括即可。
  - **改动多**（多模块、多功能点、整页重构等）：提交信息要 **更详细、字数明显增加**（约 **80～120 字或更多**），把主要改动点都写进去，例如：完成了哪些筛选/表格/弹窗、表单有哪些字段、增删改查与哪些 API 对接等，让日报能反映真实工作量。
- **重点**：说清「完成了什么」和「涉及什么」；改动多时用「包括…、…、…」把关键功能点列出来。
- **不写**：不列多文件路径清单、不写多段正文。

## 示例（AI 预期生成，日报口吻）

**改动较少时（50～80 字）：**
- `feat: 完成了新增菜单管理相关 API 接口，包括添加、删除和更新菜单等接口，并完成联调。`
- `feat: 完成了 QueryFilter 的 Option、Field 等类型定义，供筛选表单与列表页复用。`
- `fix: 完成了菜单接口参数与设备、用户 API 请求字段的修复。`

**改动较多时（80～120 字或更多，把主要点都概括进去）：**
- `feat: 完成了菜单管理页面改造：包括名称/菜单来源/客户端/菜单类型等查询筛选、树形表格展示、新增与编辑弹窗（菜单来源、类型、客户端、父级菜单、名称、路径等表单字段）、删除确认及与菜单 API 的增删改查对接。`
- `feat: 完成了用户列表页组织筛选与查询组件、分页与表格展示、以及与用户/设备 API 的对接与联调。`
