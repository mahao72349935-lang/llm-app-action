你：js 有哪些数据类型？
AI：你好，我是今天的面试官。很高兴能和你进行这次前端技术面试。我的职责是通过一系列问题来评估你的技术深度和解决问题的能力。我们开始吧。

第一个问题：JavaScript 有哪些数据类型？

你：字符串，boolean, 对象、数据、symbol、null、number
AI：好的，你提到了大部分类型，但列举得不够完整和准确。你提到了“数据”，我猜你指的是“数组”，但数组在 JavaScript 中属于对象类型的一种。

为了更严谨，你能重新完整地列举一下 **ES2020** 标准下的所有 JavaScript 数据类型吗？请区分 **原始类型（Primitive Types）** 和 **引用类型（Object）**。

你：原始数据类型 字符串 number 布尔 null   引用： object array math function
AI：你的回答有进步，区分了原始类型和引用类型，但仍然不够完整和准确。

1.  **原始类型**：你遗漏了 `undefined` 和 `Symbol`（ES6 引入），以及 `BigInt`（ES2020 引入）。
2.  **引用类型**：你提到的 `Array`、`Math`、`Function` 都属于 `Object` 类型，它们是内置的特殊对象，但本质上类型都是 `Object`。在分类时，我们通常只说 `Object` 这一种引用类型。

所以，一个更严谨的答案应该是：

**原始类型（7种）**：`String`、`Number`、`Boolean`、`Null`、`Undefined`、`Symbol`（ES6）、`BigInt`（ES2020）。
**引用类型（1种）**：`Object`（其中包含 `Array`、`Function`、`Date`、`RegExp`、`Math` 等内置对象）。

---

基于这个基础，我想追问一个很常见的问题：**如何准确判断一个变量的类型是数组（Array）？** 你能说出几种方法，并解释为什么 `typeof []` 返回 `"object"` 吗？

你：instanceof isArray prototype
AI：你好，我是今天的面试官，专注于前端技术评估。我会根据你的回答进行追问或点评，帮助展示你的技术深度。现在开始第一个问题：

请解释JavaScript中`instanceof`、`Array.isArray()`和`prototype`之间的关系，特别是在判断数组类型时的区别和最佳实践。

